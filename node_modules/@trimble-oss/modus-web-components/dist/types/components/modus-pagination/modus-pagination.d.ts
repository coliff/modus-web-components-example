import { EventEmitter } from '../../stencil-public-runtime';
export declare class ModusPagination {
  ariaLabel: string;
  activePage: number;
  maxPage: number;
  minPage: number;
  size: 'large' | 'medium' | 'small';
  /** An event that fires on page change. */
  pageChange: EventEmitter<number>;
  pages: (number | string)[];
  chevronSizeBySize: Map<string, string>;
  classBySize: Map<string, string>;
  setPages(): void;
  handleChevronClick(way: 'back' | 'forward'): void;
  handleKeydownBack(event: KeyboardEvent): void;
  handleKeydownForward(event: KeyboardEvent): void;
  handlePageClick(page: number): void;
  render(): unknown;
}
