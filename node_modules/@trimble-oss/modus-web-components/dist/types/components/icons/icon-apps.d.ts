import { FunctionalComponent } from '../../stencil-public-runtime';
interface IconProps {
  color?: string;
  onClick?: (event: MouseEvent) => void;
  size?: string;
}
export declare const IconApps: FunctionalComponent<IconProps>;
export {};
