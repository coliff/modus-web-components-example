export declare class ModusCard {
  /** (optional) The card's aria-label. */
  ariaLabel: string;
  /** (optional) The height of the card. */
  height: string;
  /** (optional) The width of the card. */
  width: string;
  render(): unknown;
}
