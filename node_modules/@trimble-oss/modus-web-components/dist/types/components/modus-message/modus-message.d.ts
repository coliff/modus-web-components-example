export declare class ModusMessage {
  /** (optional) The message's aria-label. */
  ariaLabel: string;
  /** (optional) The message's type. */
  type: 'info' | 'question';
  classByType: Map<string, string>;
  render(): unknown;
}
